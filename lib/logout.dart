import 'package:flutter/material.dart';

class Logout extends StatefulWidget {  
  @override
  LogoutLayout createState() => LogoutLayout();
}

class LogoutLayout extends State<Logout> {
  Widget build(BuildContext context) {
    return (
      Container(
        child: 
         Row(children: 
          <Widget>[
            Column(),
            Column(),
          ],)
        )
    );
  }
}